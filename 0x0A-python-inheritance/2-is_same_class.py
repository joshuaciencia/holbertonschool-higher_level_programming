#!/usr/bin/python3
"""
Module that tells whether an object is an exactly
instance of a class
"""


def is_same_class(obj, a_class):
    """
    Module that tells whether an object is an exactly
    instance of a class
    """
    return type(obj) == a_class
